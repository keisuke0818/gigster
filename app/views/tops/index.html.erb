

    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js"></script>
    <script src="/assets/jquery.autopager-1.0.0.js"></script>
    <script type="text/javascript">
        $(function() {
            $.autopager({
                load: function(current, next) {
                    var pageBreak = '<a href=?page=' + current.page + ' ></a>';
                    $(this).before(pageBreak);
                }

            });
        });
    </script>
    <script type="text/javascript">
    jQuery(function($) {

      //data-hrefの属性を持つtrを選択しclassにclickableを付加
      $('tr[data-href]').addClass('clickable')
        //クリックイベント
        .click(function(e) {
          //e.targetはクリックした要素自体、それがa要素以外であれば
          if(!$(e.target).is('a')){
            //その要素の先祖要素で一番近いtrの
            //data-href属性の値に書かれているURLに遷移する
            window.location = $(e.target).closest('tr').data('href');}
      });
    });
    </script>
    <script>(function(d, s, id) {
      var js, fjs = d.getElementsByTagName(s)[0];
      if (d.getElementById(id)) return;
      js = d.createElement(s); js.id = id;
      js.src = "//connect.facebook.net/ja_JP/all.js#xfbml=1&appId=153617311511977";
      fjs.parentNode.insertBefore(js, fjs);
    }(document, 'script', 'facebook-jssdk'));
    </script>
    <div id="fb-root"></div>
    <%= image_tag "top.jpg", :alt => 'top', :width => '1000', :height => '400', :align => 'center' %>

      <div class="navbar navbar-inverse">
        <div class="container">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
          </div>
          <div class="navbar-collapse collapse navbar-responsive-collapse">
            <ul class="nav navbar-nav">
            </ul>

            <%= search_form_for @search, :url => '/', :class => 'navbar-form navbar-left' do |f| %>

              <%= f.text_field :name_cont, :class => 'form-top-control col-lg-8', :placeholder => 'Gigs name' %>
              <%= f.country_select :country_in,nil, {:prompt => "Select Countries"}, {:class => 'form-top-control'} %>

              <%= f.date_select :day_gteq, {:default => Date.today}, {:class => 'form-top-control'} %>

              <%= f.submit 'Gig Search', :class => "btn btn-success" %>
            <% end %>

          </div><!-- /.nav-collapse -->
        </div><!-- /.container -->
      </div><!-- /.navbar -->

      <div class="content">

          <article class="hentry-home">
          <div class="container">
            <% tmp_day = false %>
            <% @gigs.each do |gig| %>


                <% if tmp_day != gig.day %>
                    <% if tmp_day != false %>
                          </tbody>
                        </table>
                      </div><!-- /example -->
                    <% end %>

                    <% tmp_day = gig.day %>
                    <div class="page-header">
                      <h3 id="tables"><%= gig.day.strftime("%m/%d(#{%w(Sun Mon Tue Wed Thu Fri Sat)[gig.day.wday]})") %></h1>
                    </div>
                    <div class="bs-example">
                      <table class="table table-striped table-bordered table-hover">
                      <tbody>
                <% end %>
                <tr data-href="/gigs/<%= gig.id %>" >
                  <td width="200" ><%= image_tag gig.flyer_url, :size => "150x150" %></td>
                  <td><h4><%= gig.name %>　
                      (<%= gig.country %>)</h4>
                      <%= simple_format(cut_off(gig.infomation, 100)) %></td>
                </tr>
            <% end %>

              </tbody>
            </table>
          </div><!-- /example -->

          </div>
          </article>

      </div>
      <% if params[:q].nil? %>
        <div style="color: #ffffff"><%= paginate @gigs %></div>
      <% end  %>






